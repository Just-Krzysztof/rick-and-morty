import React from "react";
import Logo from "../assets/logo.png";
import { Image } from "@chakra-ui/react";
const NavBar = () => {
  return <Image src={Logo} width={'30%'}/>;
};

export default NavBar;
